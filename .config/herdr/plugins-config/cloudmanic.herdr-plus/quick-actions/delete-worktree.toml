name = "Delete This Worktree"
description = "Remove the current herdr worktree (safe: refuses if dirty)"
command = 'herdr worktree remove --workspace "$HERDR_PLUS_WORKSPACE_ID"; printf "\n— done · press Enter to close (auto-closes in 15s) —\n"; read -t 15 _ </dev/tty || true'
